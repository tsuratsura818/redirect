# TASKS.md — Pivolink まわりみち

進捗は 2026-08-03 時点。実装は本フォルダ直下（Next.js 16 / npm）。

## Phase 1 — 実証MVP

### Week 1: 基盤
- [x] Next.js 16プロジェクト作成（App Router / TS strict / Tailwind v4）
- [x] 匿名セッション（署名cookie）実装 `src/lib/session.ts`
- [x] 回廊ルーティングエンジン `src/lib/routing.ts`（純関数・I/Oなし）
- [x] ルーティングのユニットテスト（3目的地×1000周×3シード、二択破綻0・ユニークルート400+）
- [x] シードデータ（キャンペーン1・目的地3・スポット8 ※許諾未取得の仮データ）
- [x] シードSQL生成 `npm run gen:seed` → `supabase/migrations/0002_seed_kyoto.sql`
- [ ] **Supabase接続**（プロジェクト作成＋`0001_init.sql`/`0002_seed_kyoto.sql` 適用）
      — 未実施。env未設定の間はインメモリのデモモードで動く

### Week 2: 参加者UI（モックアップ移植）
- [x] `/s/[qrToken]` QR着地→スタート/スポット分岐
- [x] 目的地選択画面（プリセット3件。自由入力は Phase 2）
- [x] スポット画面（物語・ナビキャラ・スタンプ演出）
- [x] 「次の寄り道へ向かう」画面（区間ナビのみ・目的地座標は最終区間まで出さない）
- [x] 距離バー（目的地まで残り◯m + プログレス）
- [x] スタンプ帳 `/book`・到着画面 `/arrival`（歩行距離/最短/まわりみち率）
- [x] ルートマップ（歩いた道のりと最短ルートを重ねたSVG。到着画面とスタンプ帳）
- [x] クーポン発行（到着時にコード生成）
- [x] ja/en 2言語（セッションに保存。zh/ko は Phase 2）
- [x] 構造化データ（TouristAttraction + BreadcrumbList）・robots.txt でAIクローラー許可

### Week 3: 管理・計測
- [x] 管理ダッシュボード `/admin`: スポット重み・混雑度・公開の即時反映
- [x] 管理認証（パスワード + 署名cookie。本番でenv未設定なら fail-closed）
- [x] 回遊レポート（セッション/完走/スタンプ数/まわりみち率中央値/提示無視率/ナビ利用率/直近ルート）
- [x] QRトークン発行 + 入稿用PNG出力 `/admin/qr`
- [x] ルーティングのプレビューAPI `/api/route-choices`（管理者のみ・重み調整の確認用）
- [x] 外部デジタルマップ連携（スポット別 `map_url` + `MAP_NAV_URL_TEMPLATE`。MapPenguin等）
- [ ] **Vercelデプロイ** — 未実施。★**Supabase接続が前提**。
      インメモリのままVercelへ出すとセッションが保持されない（serverlessはリクエストごとに
      別インスタンスになりうるため、目的地とスタンプが消える）。ローカルの `npm start` は単一
      プロセスなので問題なく通しデモできる
- [ ] Lighthouse Performance/Accessibility 90+ の計測 — 未実施

### 受け入れ基準（docs/requirements.md §5）の状況
| # | 基準 | 状況 |
|---|---|---|
| 1 | QR着地→目的地選択→5スポット→到着まで完走 | ✅ `npm run e2e` で自動確認 |
| 2 | 3目的地×1000周で二択破綻0・ユニークルート400+ | ✅ `npm test`（実測 408〜440/清水寺・ピーク帯） |
| 3 | 同一スポット再スキャンでスタンプが増えない | ✅ e2eで確認（本番はDB一意制約で担保） |
| 4 | 最終区間まで目的地座標が露出しない | ✅ e2eで確認 |
| 5 | 管理画面で重み変更→次の抽選に即時反映 | ✅ `npm run e2e:admin` で確認 |
| 6 | レポートで分散状況とまわりみち率が見える | ✅ `/admin` |
| 7 | Lighthouse 90+ | ⬜ 未計測 |

## Phase 2 — エリア展開
- [ ] 混雑連動ルール（スキャンログ密度→重み自動補正）※engineは `congestion` ルールとして実装済・データ未接続
- [ ] スポンサーCM（動画枠・スキップ・頻度制御・impressionログ）
- [ ] zh/ko追加・目的地自由入力（ジオコーディング）
- [ ] 月次回遊レポートのPDF/CSV出力（補助金申請の根拠資料フォーマット）
- [ ] ナビキャラ差し替え機構（キャラ別ルート個性 = context ルール）※engine側は実装済

## Phase 3 — SaaS化・横展開
- [ ] キャンペーン セルフサーブ作成UI（他都市: 鎌倉・金沢・奈良・日光）
- [ ] マルチテナント化（organization → campaign 階層）
- [ ] Stripe課金（Pivolink既存ティアに Tourism プラン追加）
- [ ] Tsumugi連携（クーポン/CRM）

## 対外公開の前に必ず潰すこと
- [ ] 実在スポット（六波羅蜜寺ほか8箇所）・d食堂特典の**許諾取得 or 差し替え**
- [ ] 「まわりみち」商標のJ-PlatPat確認・出願
- [ ] QRトークンのURLスキーム確定（印刷入稿はこの後）※`dev-*` の固定トークンは本番で再発行する
- [ ] ナビキャラ「案内狐・コン」の確定（コラボキャラ）
